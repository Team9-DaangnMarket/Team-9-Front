import React from 'react';
import { Button } from '../elements'

const Login = () => {
  return (
      <div>
        <Button version={'orange'}>로그인</Button>
      </div>
  );
};

export default Login;
